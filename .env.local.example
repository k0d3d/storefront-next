# WooCommerce API URL (server-side only - not exposed to client)
# All data fetching happens in Server Components
# For local development
WC_API_URL=http://localhost:5000

# For production (set in Netlify Dashboard → Environment Variables)
# WC_API_URL=https://wc-api.tohju.com
